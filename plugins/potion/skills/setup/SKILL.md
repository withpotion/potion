---
name: setup
description: Connect the Potion connector, sign the user in, and get their feed into a podcast app. Use this when Potion's tools are missing or every call is refused, when a call reports there is no account, when the user has just installed this plugin, or when they are holding a feed URL and do not know what to do with it. Use it too when someone asks how to subscribe, where to paste the RSS URL, or why a new episode has not shown up on their phone.
---

Read `${CLAUDE_PLUGIN_ROOT}/SETUP.md` and follow it. It covers the three things that happen once:
authorizing the connector over OAuth, making sure an account exists, and adding the feed in
whatever podcast app the user has.
